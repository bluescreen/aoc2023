import { readInputForDayExample } from "../util.ts";

export const part1 = (input: string[]): number => {
  console.log(input);

  return 0;
};

const data = await readInputForDayExample(3);
console.log("Result part 1", part1(data));
